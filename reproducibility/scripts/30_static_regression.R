# Static regression manuscript examples (regMod, exal_static_LDVB, exal_static_mcmc).
# Add exact code used for static examples and comparison tables.

log_step("30_static_regression.R: placeholder (add manuscript static regression code).")

# Suggested stable outputs:
# - figures in Figures/
# - summary tables in reproducibility/outputs/
