# Manuscript Tracker

This folder contains the manifest/tracker step for the manuscript workflow.

`run.R` records which figures, tables, logs, backend profiles, and
reproducibility notes were produced by a run. It should run after the example
scripts and should normally not need example-specific edits.
